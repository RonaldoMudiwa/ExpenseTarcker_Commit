[pytest]
# Run pytest from the project root and it will find the package on sys.path.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
